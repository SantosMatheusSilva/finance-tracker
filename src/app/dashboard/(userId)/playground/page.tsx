'use client'
//import OverviewPageSkeleton from "@/app/ui/dashboard/overview-page-skeleton";
import PlannerModal from "@/app/ui/dashboard/planer/planner-modal";

export default function ComponentsTestingPage () {
      
    return (
        <PlannerModal isOpen={true} onOpenChange={() => {}} />
    )
}